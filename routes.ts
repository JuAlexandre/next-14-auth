export const publicRoutes: string[] = ['/'];

export const authRoutes: string[] = ['/sign-in', '/error', '/verify-request'];

export const apiAuthPrefix: string = '/api/auth';

export const DEFAULT_REDIRECT: string = '/dashboard';
